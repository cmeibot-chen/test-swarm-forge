# issue-1-clamp

Type: utility

GitHub Issue #1: Add clamp utility. Implement test-swarm-forge.clamp/clamp in src/test_swarm_forge/clamp.clj, with inclusive bounds and ex-info when lower exceeds upper. Add Speclj coverage for below, inside, above, equal bounds, and reversed bounds. Preserve runtime files.
